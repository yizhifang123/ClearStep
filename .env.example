# Copy this file to .env and fill in your key to enable live analysis of custom text.
# Without a key, the app still runs in DEMO MODE on the bundled examples.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model (default is a fast, capable Claude model).
# CLEARSTEP_MODEL=claude-sonnet-4-6
