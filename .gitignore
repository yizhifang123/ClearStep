# Data — licensed / large; do NOT commit raw EEG (see data/download.md)
data/raw/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.ruff_cache/

# Large intermediate feature table (regenerated by ml/train.py).
# NOTE: ml/artifacts/model.pkl (11 KB) IS committed so a clone/deploy runs the
# real-case page without needing the raw EEG (which we cannot redistribute).
data/features/*_epochs.csv

# OS
.DS_Store
.gstack/
